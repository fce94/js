/*----------------  VARIABLE ----------------*/
// ? NB : les noms de variable sont sensibles a la czsse

// Déclaration de variable
let uneVarible;
let UNEVARIBLE;

// AFFECTATION d'une varubake (dinne une valeur à la variale)
uneVariable = 'Je suis une Variable'

// Les deux peuvent se faire en même temps
let hello = 'Hello, word'

hello = uneVariable
console.log(hello)

// Le mot clé const permet de déclarer une constante
// Vous ne pouvez pas modifier une constante ( on dit qu'elle est immuable )
// `const` va garantir que la valeur ne soit pas modifié tout au long de l'exeption du script
const constante = 'je suis une constante';
constante = ' autre chose'

// Le mot clé var permet aussi de déclarer des variables
// C'est une pratique desuète ( c'etait la seule facon avant 2015 )
var past = 'je suis dépassé'